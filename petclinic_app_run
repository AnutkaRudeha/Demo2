FROM amazonlinux:2023

ARG APPUSER=app_user
ENV APP_USER=$APPUSER

RUN yum update -y && yum install -y java-17-amazon-corretto && \
    yum clean all && \
    rm -rf /var/cache/yum

RUN yum -y install python3 \
    python3-pip \
    shadow-utils

# RUN mkdir run_java_project
# COPY /home/runner/work/Demo2/Demo2/*.jar /run_java_project

RUN echo -e $APPUSER 
RUN useradd $APPUSER && \
    chown $APPUSER *.jar 

USER $APPUSER
WORKDIR /home/$APPUSER

EXPOSE 8080
# ENTRYPOINT ["java","-jar","/run_java_project/spring-petclinic-2.3.1.BUILD-SNAPSHOT.jar"]
